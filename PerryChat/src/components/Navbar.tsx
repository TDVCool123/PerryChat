import { Box } from '@mui/material';

export const Navbar = () => {
    return (
        <Box
          sx={{
            height: 50,
            backgroundColor: 'primary.dark',
    
          }}
        />
      );
}
